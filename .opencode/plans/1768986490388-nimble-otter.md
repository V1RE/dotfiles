# Plan: Remove avante.nvim Traces

## Summary
The main `avante.lua` plugin spec was deleted in commit `e7b1d18` (2025-11-12), but leftover traces remain in two files.

## Files to Modify

### 1. `dot_config/nvim/.lazy-lock.json` (line 5)
- Remove the `avante.nvim` entry from the lockfile
- This will be cleaned up automatically after running `:Lazy sync`, but can also be manually removed

### 2. `dot_config/nvim/exact_lua/core/exact_plugins/blink.lua`
- **Lines 85-87**: Remove `"avante_files"`, `"avante_mentions"`, `"avante_commands"` from the `default` sources list
- **Lines 200-245**: Remove the `avante_commands`, `avante_files`, and `avante_mentions` provider definitions

## Implementation Steps

1. Edit `blink.lua`:
   - Remove avante sources from `sources.default` array
   - Remove the three avante provider configurations

2. Edit `.lazy-lock.json`:
   - Remove the `avante.nvim` line entry

3. Optionally clean up by running `:Lazy sync` in Neovim to update the lockfile automatically

## Verification
1. Open Neovim
2. Run `:Lazy` - avante.nvim should not appear in the plugin list
3. Trigger completion in a buffer - no errors about missing avante providers
4. `rg -i avante` in the repo should return no results
